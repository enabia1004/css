$(function(){
$(".list").slick({
    autoplay:true,
    // 한화면에 스크롤보일 개수
    slidesToShow:2,
    // 스크롤할때 얼마나 나가는지 설정
    slidesToScroll:1,
    // 좌우버튼숨기기
    arrows:false,
    // 페이지 버튼 보이기
    dots:true
});
});
